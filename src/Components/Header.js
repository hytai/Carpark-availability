import { Icon } from '@iconify/react-with-api'

const Header = () => {
    return (
        <header className= "header">
            <h1> <Icon icon = "icon-park-outline:parking" className = "header-icon"/> Carpark availability </h1>
        </header>
    )
}

export default Header